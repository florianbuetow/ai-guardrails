name: MMIX template
on:
  push:
    paths:
      - 'blueprints/mmix-cli-base/**'
      - 'violations/mmix-cli-base/**'
      - 'tests/**'
      - '.github/workflows/mmix.yml'
  pull_request:
    paths:
      - 'blueprints/mmix-cli-base/**'
      - 'violations/mmix-cli-base/**'
      - 'tests/**'
      - '.github/workflows/mmix.yml'
permissions:
  contents: read
jobs:
  template:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Preserve symbolic links on Windows checkout
        if: runner.os == 'Windows'
        run: git config --global core.symlinks true
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: extractions/setup-just@v3
      - uses: ilammy/msvc-dev-cmd@v1
        if: runner.os == 'Windows'
      # Copier is a generation prerequisite, never a generated-project tool.
      - run: python -m pip install copier==9.17.2
      - name: Fresh project baseline and violation tests
        run: python tests/test_mmix.py
