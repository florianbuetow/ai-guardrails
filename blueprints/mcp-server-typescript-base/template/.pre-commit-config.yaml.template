repos:
  - repo: local
    hooks:
      - id: run-just-ci-quiet
        name: Run just ci-quiet
        entry: just ci-quiet
        language: system
        pass_filenames: false
        always_run: true
        verbose: true
